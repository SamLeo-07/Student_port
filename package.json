{
  "name": "student-portal-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others -n \"frontend,backend\" -c \"cyan,green\" \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "clean": "taskkill /F /IM node.exe /T",
    "install:all": "npm install",
    "start:backend": "npm start --workspace=backend",
    "start:frontend": "npm run build --workspace=frontend && npm run preview --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
